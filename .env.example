# The PMS backend. No trailing slash.
VITE_API_URL=http://localhost:5000

# Only needed if the PMS is served from a subpath rather than its own host.
# Must start and end with a slash. Leave unset for a root deployment.
# VITE_BASE=/pms/
